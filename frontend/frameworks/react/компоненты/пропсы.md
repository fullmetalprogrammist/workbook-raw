---
title: "Пропсы компонентов React"
layout: default
parent: "Компоненты React"
---



# Наши пропсы

- в этом компоненте title, price, inStock - это пропсы ("наши")

```jsx
<ProductCard 
  title="iPhone 15"
  price={999} 
  inStock={true} 
/>
```

- реакт собирает их в единый объект и передает через *первый* параметр `props` в функцию компонента
  - обычно для удобства этот параметр деструктурируют

```jsx
// function ProductCard(props) {  // <-- Все пропсы собраны в едином объекте
function ProductCard({ title, price, inStock }) {  // <-- Деструктурируем его для удобства
  return (
    <div className="product-card">
      <h3>{title}</h3>
      <p className="price">${price}</p>
      <p className="stock">
        {inStock ? '✅ In stock' : '❌ Out of stock'}
      </p>
    </div>
  );
}
```



# Стандартные пропсы

## children

```jsx
<FancyButton variant="secondary">
  <span>⭐</span> Rate Product <span>⭐</span>  {/* Все, что вложено в FancyButton, собирается в children  */}
</FancyButton>
```

```jsx
<FancyButton variant="primary">
  🛒 Add to Cart  {/* Это тоже попадает в children */}
</FancyButton>
```

- через children получаем все, что в компонент напихал клиент, и вставляем как есть:

```jsx
function FancyButton({ variant = "primary", children }) {  // <-- children отлично соседствует с "нашими" пропсами
  return (
    <button className={`btn btn-${variant}`}>  {/* С нас - только обертка */}
      {children}  {/* Контент - на совести клиента нашего компонента */}
    </button>
  );
}
```





# Проброс пропсов

- хотим сделать форму с кастомными полями ввода

```jsx
function LoginForm() {
  return (
    <form>
      <FormField
        label="Email"
        type="email"
        name="email"
        placeholder="Введите email"
        required
        autoComplete="email"
        onChange={(e) => console.log(e.target.value)}
      />
      
      <FormField
        label="Пароль"
        type="password"
        name="password"
        placeholder="Введите пароль"
        minLength="6"
        autoComplete="current-password"
        onFocus={() => console.log('фокус на пароле')}
      />
      
      <FormField
        label="Телефон"
        type="tel"
        name="phone"
        pattern="[0-9]{10}"
        title="10 цифр номера"
        defaultValue="+7"
      />
    </form>
  );
};
```

- код кастомного поля ввода:

```jsx
function FormField({ label, error, ...restProps }) {  // <-- Что нам не надо, собираем обратно в кучу
  return (
    <div className="form-field">
      <label>{label}</label>
      <input {...restProps} />  {/* И передаем туда, где оно надо */}
      {error && <span className="error">{error}</span>}
    </div>
  );
};
```

- "кастомность" заключается по сути в том что мы добавляем label рядом с полем и отображаем ошибку, если она передана
  - а все остальные настройки поля, вроде типа (email, пароль и т.д.), реакции на изменение и т.д. оставляем на заботу клиента компонента
  - соответственно, для себя нам надо только label и error, но мы не можем игнорировать все остальные настройки, которые передал клиент
    - поэтому мы собираем все пропсы, которые лично нам не нужны, в отдельную переменную с помощью rest-оператора и прокидываем дальше в input
    - т.о. и все что нам надо получаем, и что клиент передал не теряется и уходит как есть дальше